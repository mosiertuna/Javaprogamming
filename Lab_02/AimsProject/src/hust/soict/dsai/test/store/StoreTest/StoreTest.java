package hust.soict.dsai.test.store.StoreTest;
//import hust.soict.dsai.Aims.media.DigitalVideoDisc;
//import hust.soict.dsai.Aims.store.Store.*;


public class StoreTest {
  public static void main(String[] args) {
    /*
      // Tạo một đối tượng Store
      Store store = new Store();

      // Tạo một vài đối tượng DigitalVideoDisc
      DigitalVideoDisc disc1 = new DigitalVideoDisc("The Lion King", "Animation", "Roger Allers", 87, 19.95f);
      DigitalVideoDisc disc2 = new DigitalVideoDisc("Star Wars", "Science Fiction", "George Lucas", 124, 24.95f);
      DigitalVideoDisc disc3 = new DigitalVideoDisc("Aladin", "Animation", 18.99f);

      // Thêm các đĩa DVD vào Store
      store.addDVD(disc1);
      store.addDVD(disc2);
      store.addDVD(disc3);

      // Kiểm tra số lượng đĩa DVD đã đặt hàng
      //System.out.println("VuQuangDung-20225818 - So luong dia DVD da dat hang: " + store.qtyOrdered);

      // Thử xóa một đĩa DVD
      store.removeDVD(disc2);

      // Kiểm tra lại số lượng đĩa DVD đã đặt hàng
      //System.out.println("VuQuangDung-20225818 - So luong dia DVD da dat hang: " + store.qtyOrdered);

      // Thử xóa một đĩa DVD không có trong Store
      store.removeDVD(new DigitalVideoDisc("Harry Potter", "Fantasy", 22.95f));

      */
  }
}
